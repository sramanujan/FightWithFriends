DrawableObject = function(type) {
    switch(type) {
        case 'tower':break;
        case 'unit':break;
        case 'projectile':break;
    }
}

GlobalGraphics = {};
GlobalGraphics.init = function() {
    
}